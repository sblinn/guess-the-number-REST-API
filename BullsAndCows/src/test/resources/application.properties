spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/BullsAndCowsDBTest?serverTimezone=America/Chicago&useSSL=false
spring.datasource.username=root
spring.datasource.password=N07$m4r7

# the next line is for suppressing spring boot console output, 
# so that we only see our own output.
#logging.pattern.console=

# Set the active dependency profile as "database". 
spring.profiles.active=database
